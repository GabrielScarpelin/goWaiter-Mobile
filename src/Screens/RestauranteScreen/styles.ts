import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: 'white'
  },
  textTitle: {
    fontSize: 24,
    marginTop: 32,
    marginLeft: 40,
    fontWeight: '900',
    marginRight: 40
  }
});